import 'package:flutter/material.dart';

class AppOffsets {
  static const Offset routingAnimationBeginningOffset = Offset(0.0, 1.0);
  static const Offset routingAnimationEndingOffset = Offset.zero;

  static const Offset homeAppBarBoxShadowOffset = Offset(4, 8);
}
